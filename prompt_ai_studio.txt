Generame UN SOLO ARCHIVO HTML COMPLETO y autónomo (sin React, sin TypeScript, sin módulos, sin build tools, sin imports externos excepto Chart.js) para una web app mobile-first de un purificador de aire hexagonal con planta marina. Usa un ESP32 con DHT11, TCS230, backend Node.js, y Chart.js.

CRITICO: El output debe ser UN UNICO ARCHIVO .html que funcione solo. Todo el CSS dentro de <style> y todo el JS dentro de <script>. NADA de TypeScript, React, módulos ES, bundlers, ni imports de archivos.

STYLE & AESTHETICS:
- Minimalist, inspired by Apple, Tesla, and scientific dashboards
- Light theme palette:
  - Primary dark: #37371B (deep olive)
  - Secondary: #AFAE90 (muted green-beige)
  - Background: #F0EADE (soft cream)
- Soft shadows, subtle depth
- Rounded cards (border-radius: 16px)
- Thin line icons (Unicode or simple SVG, no filled icons)
- System fonts: -apple-system, BlinkMacSystemFont, 'Inter', sans-serif
- Calm, breathable spacing (padding 20-24px)

INTERFACE - 3 pantallas SPA con navegacion inferior tipo app:

PANTALLA 1 - DASHBOARD:
- Gauge circular animado para el pH que cambia de color segun estado
- Temperatura en grande con icono de termometro
- Humedad en grande con icono de gota
- Estado: "Saludable" (verde), "Advertencia" (naranja), "Critico" (rojo)
- Ultima actualizacion con timestamp
- Auto-refresh cada 5 segundos con fetch a /latest

PANTALLA 2 - HISTORIAL:
- Botones para seleccionar: ultimas 10, 25 o 50 lecturas
- Chart.js line chart con 3 lineas: temp (rojo #E76F51), humedad (azul #457B9D), pH (verde #52B788)
- Eje X con hora:minuto
- Cards con estadisticas: minimo, maximo, promedio de cada variable
- Fetch a /history con el limite seleccionado

PANTALLA 3 - ACERCA DE:
- Hexagono con CSS
- Lista de componentes: ESP32, DHT11, TCS230, planta marina
- Texto explicativo breve

API ENDPOINTS (ya existen en el backend):
- GET /latest → { temperatura, humedad, ph, phStatus, colorR, colorG, colorB, timestamp }
- GET /history?limit=50 → array de objetos con los mismos campos

USA: Chart.js CDN (https://cdn.jsdelivr.net/npm/chart.js), fetch, CSS Grid/Flexbox.
NO uses: React, TypeScript, Vue, Svelte, modules, imports, build tools, npm.
Todo el texto en espanol. Sin comentarios en el codigo.

DEVUELVE SOLO EL HTML COMPLETO, sin explicaciones ni markdown.
